<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sparkforchange">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".controllers.ToolbarDrawerActivity" />
        <activity android:name=".controllers.ProfileActivity" />
        <activity android:name=".controllers.SpendSparksActivity" />
        <activity android:name=".controllers.DonateActivity" />
        <activity android:name=".controllers.ServeActivity" />
        <activity android:name=".controllers.GroupActivity" />
        <activity android:name=".controllers.RewardActivity" />
        <activity android:name=".controllers.ChooseCharityActivity" />
        <activity android:name=".controllers.PaymentMethodActivity" />
        <activity android:name=".controllers.TimelineActivity" />
        <activity android:name=".controllers.LogVolunteerActivity" />
        <activity android:name=".controllers.QRActivity"></activity>
        <activity android:name=".controllers.CharityActivity">
        </activity>
</application>

</manifest>